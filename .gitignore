# Deny by default under secrets/ so a plaintext file staged while assembling a
# secret can never be committed, whatever its name or nesting depth.
secrets/**
!secrets/**/
!secrets/**/*.age

# Flake outputs
result
result-*

# direnv
.direnv/
.envrc

.keys/
